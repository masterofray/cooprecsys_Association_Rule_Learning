[build-system]
requires = ["setuptools", "wheel", "Cython"]
build-backend = "setuptools.build_meta"

[project]
name = "cfptree"
version = "2.0.0"
description = "CFP-tree structure for efficient frequent pattern mining"
requires-python = ">=3.7"